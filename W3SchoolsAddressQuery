//Version of the query created for input to W3 Schools to select the Address for the Supplier of ProductID 40
SELECT SupplierName, ProductName, Address, ProductID, s.SupplierID
FROM Products, Suppliers AS s
WHERE ProductID = 40
AND s.SupplierID = 19;
